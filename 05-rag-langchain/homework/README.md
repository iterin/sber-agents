# Домашнее задание №5: RAG с LangChain

## 🎯 Описание задания

В этом задании вы создадите RAG (Retrieval-Augmented Generation) систему для Telegram-бота, способную отвечать на вопросы пользователей на основе документов. Вы научитесь работать с LangChain, векторными базами данных, эмбеддингами и освоите техники индексации различных типов данных.

**Цели:**
- Освоить построение RAG-систем с использованием LangChain
- Научиться индексировать документы и работать с векторными хранилищами
- Экспериментировать с различными стратегиями разбиения документов на чанки
- Работать с различными типами данных (PDF и JSON)
- Интегрировать RAG в Telegram-бота

**Важно:**
> **Версии задания:** Вы можете выбрать одну из двух версий задания в зависимости от вашего опыта и предпочтений:
> - **Базовая версия** — доработка проекта из папки `live/`
> - **Расширенная версия** — AI-driven разработка проекта с нуля

---

## Общие шаги для обеих версий

### Шаг 0: Изучение материалов

**Что нужно сделать:**

1. **Изучите или запустите ноутбук с примерами:**
   - Файл `05-rag-langchain/slides/naive-rag-notebook/naive-rag.ipynb`
   - Это базовый пример RAG, который показывает основные концепции

2. **Изучите live-проект:**
   - Посмотрите реализацию в `05-rag-langchain/live/`
   - Обратите внимание на структуру проекта и RAG-пайплайн
   - Изучите как работает индексация в `src/indexer.py`
   - Посмотрите на реализацию RAG-цепочек в `src/rag.py`

3. **Изучите данные:**
   - PDF документы с условиями по кредитам и вкладам в `live/data/`
   - JSON файл с вопросами-ответами про карты: `sberbank_help_documents.json`

---

### Шаг 1: Подготовка рабочего пространства

**Что нужно сделать:**

1. Перейдите в ваш репозиторий `sber-agents`:
   ```bash
   cd путь/к/вашему/sber-agents
   ```

2. Создайте папку `05-rag-langchain` в корне репозитория (если её ещё нет):
   ```bash
   mkdir -p 05-rag-langchain
   cd 05-rag-langchain
   ```

**Далее выберите одну из версий задания:**
- **[Базовая версия](#вариант-базовый-доработка-существующего-проекта)** — если хотите доработать существующий проект
- **[Расширенная версия](#вариант-расширенный-разработка-с-нуля)** — если хотите создать новый проект с нуля

---

## Вариант БАЗОВЫЙ: Доработка существующего проекта

### Шаг 2Б: Копирование проекта

**Что нужно сделать:**

1. Скопируйте файлы проекта из директории `live`:
   ```bash
   # Находясь в папке 05-rag-langchain
   cp -r ../05-rag-langchain/live/* .
   ```

2. Откройте эту папку как **новый проект** в Cursor (или вашей IDE):
   - File → Open Folder → выберите папку `05-rag-langchain`

---

### Шаг 3Б: Настройка и запуск

**Что нужно сделать:**

#### 3.1. Получение API ключа Fireworks

В этот раз мы воспользуемся провайдером **Fireworks.ai** вместо OpenRouter.

**Почему Fireworks?**
- Новому аккаунту выделяется $1 бесплатного кредита
- Этого хватит для выполнения всего домашнего задания
- Поддерживает модели для текста и эмбеддинги
- Совместим с OpenAI API

**Регистрация:**
1. Перейдите на [Fireworks.ai](https://fireworks.ai/)
2. Зарегистрируйтесь (можно через GitHub)
3. Перейдите в раздел API Keys
4. Создайте новый API ключ
5. Скопируйте ключ (он начинается с `fw_...`)

#### 3.2. Настройка окружения

1. **Создайте `.env` файл на основе примера:**
   ```bash
   cp env.example .env
   ```

2. **Отредактируйте `.env` файл для работы с Fireworks:**
   ```bash
   # Telegram
   TELEGRAM_TOKEN=your_telegram_bot_token

   # Fireworks API
   OPENAI_API_KEY=fw_your_api_key_here
   OPENAI_BASE_URL=https://api.fireworks.ai/inference/v1
   MODEL=accounts/fireworks/models/gpt-oss-120b
   MODEL_QUERY_TRANSFORM=accounts/fireworks/models/gpt-oss-120b
   EMBEDDING_MODEL=accounts/fireworks/models/qwen3-embedding-8b

   # Пути
   DATA_DIR=data
   PROMPTS_DIR=prompts
   CONVERSATION_SYSTEM_PROMPT_FILE=conversation_system.txt
   QUERY_TRANSFORM_PROMPT_FILE=query_transform.txt

   # Системный промпт
   SYSTEM_PROMPT=Ты ассистент Сбербанка, отвечающий на вопросы по документам.
   ```

   > **Примечание:** Не забудьте создать своего Telegram бота через @BotFather и получить токен!

#### 3.3. Установка и запуск

1. **Установите зависимости:**
   ```bash
   make install
   ```

2. **Запустите бота:**
   ```bash
   make run
   ```

#### 3.4. Тестирование

1. **Откройте диалог с вашим ботом в Telegram**

2. **Протестируйте работу с вопросами по кредитам и вкладам:**
   - Отправьте: "Какие условия потребительского кредита?"
   - Отправьте: "Какие виды вкладов есть?"
   - Отправьте: "Какая процентная ставка по вкладу Сохраняй?"

3. **Убедитесь, что бот корректно отвечает на основе документов**

4. **Проверьте реакцию на вопросы вне темы:**
   - Отправьте: "Какая погода сегодня?"
   - Бот должен ответить, что не нашёл информации в документах

**Чеклист результата:**
- ✅ Бот запущен и работает с Fireworks API
- ✅ Документы проиндексированы
- ✅ Бот отвечает на вопросы по кредитам и вкладам
- ✅ Бот корректно реагирует на вопросы вне контекста

---

### Шаг 4Б: Эксперименты с индексацией PDF

**Что нужно сделать:**

Сейчас в проекте используется простое разбиение на чанки с фиксированным размером (`chunk_size=500`). Попробуем другие подходы и сравним результаты.

#### 4.1. Текущая реализация

Откройте `src/indexer.py` и посмотрите на текущую функцию разбиения:

```python
def split_documents(pages: list) -> list:
    """Разбиение документов на чанки"""
    text_splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=50
    )
    chunks = text_splitter.split_documents(pages)
    logger.info(f"Split into {len(chunks)} chunks")
    return chunks
```

#### 4.2. Эксперимент 1: Разные размеры чанков

**Цель:** Понять как размер чанка влияет на качество ответов.

**Обоснование:** PDF документы от Сбербанка содержат структурированную информацию. Маленькие чанки (500 символов) могут разбивать логически связанную информацию, а большие чанки (1500 символов) могут захватывать больше контекста, но быть менее релевантными.

**Код для эксперимента:**

```python
def split_documents(pages: list) -> list:
    """Разбиение документов на чанки - вариант с большими чанками"""
    text_splitter = RecursiveCharacterTextSplitter(
        chunk_size=1500,      # Увеличили в 3 раза
        chunk_overlap=150     # Увеличили overlap пропорционально
    )
    chunks = text_splitter.split_documents(pages)
    logger.info(f"Split into {len(chunks)} chunks")
    return chunks
```

**Что попробовать:**
1. Измените `chunk_size` на `1500` и `chunk_overlap` на `150`
2. Перезапустите бота (он переиндексирует документы)
3. Задайте те же самые вопросы про кредиты и вклады
4. Сравните качество ответов

**Запишите наблюдения:**
- Сколько чанков получилось? (было ~377 при chunk_size=500)
- Изменилось ли качество ответов?
- Стали ли ответы более/менее детальными?
- Сохранилась ли релевантность?

#### 4.3. Эксперимент 2: Сепараторы с учетом структуры документа

**Цель:** Попробовать разбиение с учетом структуры PDF документа.

**Обоснование:** Документы Сбербанка имеют четкую структуру с разделами и пунктами. Использование специальных сепараторов может помочь сохранить логическую целостность информации.

**Код для эксперимента:**

```python
def split_documents(pages: list) -> list:
    """Разбиение документов с учетом структуры"""
    # Сепараторы для банковских документов
    # Пробуем разбивать по: двойным переносам строк, одинарным, пробелам
    text_splitter = RecursiveCharacterTextSplitter(
        chunk_size=800,
        chunk_overlap=100,
        separators=[
            "\n\n\n",    # Тройной перенос - обычно разделы
            "\n\n",      # Двойной перенос - параграфы
            "\n",        # Одинарный перенос
            ". ",        # Конец предложения
            " ",         # Пробелы
            ""           # Символы
        ],
        keep_separator=True  # Сохраняем разделители для контекста
    )
    chunks = text_splitter.split_documents(pages)
    logger.info(f"Split into {len(chunks)} chunks")
    return chunks
```

**Что попробовать:**
1. Замените функцию `split_documents()` на версию выше
2. Перезапустите бота
3. Задайте вопросы про кредиты и вклады
4. Сравните результаты

**Запишите наблюдения:**
- Как изменилось количество чанков?
- Стали ли ответы более структурированными?
- Сохраняется ли контекст лучше при таком разбиении?

#### 4.4. Дополнительные эксперименты (опционально)

Попробуйте другие комбинации параметров:
- `chunk_size=300, chunk_overlap=50` — маленькие чанки
- `chunk_size=1000, chunk_overlap=200` — средние чанки с большим overlap
- Разные наборы сепараторов

**Важно:** Запишите свои наблюдения в текстовый файл или в отчёт. Это пригодится на шаге подготовки финального отчёта.

---

### Шаг 5Б: Добавление JSON датасета с вопросами-ответами

**Что нужно сделать:**

Сейчас бот индексирует только PDF документы. Добавим новый источник данных — JSON файл с готовыми парами вопрос-ответ про дебетовые и кредитные карты.

#### 5.1. Изучите JSON файл

Откройте файл `data/sberbank_help_documents.json` и посмотрите на структуру:

```json
[
  {
    "question": "Как заказать карту?",
    "answer": "Вы можете заказать карту: на сайте банка; в СберБанк Онлайн...",
    "category": "Вопросы о дебетовых картах",
    "full_text": "Категория: Вопросы о дебетовых картах\n\nВопрос: Как заказать карту?\n\nОтвет: ..."
  },
  ...
]
```

#### 5.2. Создайте новый файл индексации

Создайте файл `src/indexer_with_json.py` на основе `src/indexer.py`:

```bash
cp src/indexer.py src/indexer_with_json.py
```

#### 5.3. Добавьте загрузку JSON

**Концепция:** Каждая пара вопрос-ответ из JSON будет одним чанком. Это логично, так как это уже готовые атомарные единицы информации.

**Добавьте импорт JSONLoader:**

```python
from langchain_community.document_loaders import JSONLoader
```

**Добавьте функцию загрузки JSON:**

```python
def load_json_documents(json_file_path: str) -> list:
    """
    Загрузка документов из JSON файла с вопросами-ответами
    Каждая пара Q&A становится отдельным чанком
    """
    from pathlib import Path
    
    json_path = Path(json_file_path)
    if not json_path.exists():
        logger.warning(f"JSON file {json_file_path} does not exist")
        return []
    
    # JSONLoader с jq_schema для извлечения full_text из каждого элемента массива
    loader = JSONLoader(
        file_path=str(json_path),
        jq_schema='.[].full_text',  # Извлекаем full_text из каждого элемента
        text_content=False
    )
    
    documents = loader.load()
    logger.info(f"Loaded {len(documents)} Q&A pairs from JSON")
    return documents
```

**Пояснение к коду:**
- `jq_schema='.[].full_text'` — это JQ-фильтр, который берёт каждый элемент массива (`[]`) и извлекает из него поле `full_text`
- Каждая запись JSON превращается в отдельный Document
- Чанкинг не нужен, так как Q&A пары уже атомарны

#### 5.4. Обновите функцию `reindex_all()`

Измените функцию, чтобы она загружала и PDF, и JSON:

```python
async def reindex_all():
    """Полная переиндексация всех документов (PDF + JSON)"""
    logger.info("Starting full reindexing...")
    
    try:
        # 1. Загружаем и обрабатываем PDF документы
        pdf_pages = load_pdf_documents(config.DATA_DIR)
        if not pdf_pages:
            logger.warning("No PDF documents found to index")
        
        pdf_chunks = split_documents(pdf_pages) if pdf_pages else []
        
        # 2. Загружаем JSON с вопросами-ответами
        json_file = f"{config.DATA_DIR}/sberbank_help_documents.json"
        json_chunks = load_json_documents(json_file)
        
        # 3. Объединяем все чанки
        all_chunks = pdf_chunks + json_chunks
        
        if not all_chunks:
            logger.warning("No documents found to index")
            return None
        
        logger.info(f"Total chunks to index: {len(all_chunks)} (PDF: {len(pdf_chunks)}, JSON: {len(json_chunks)})")
            
        # 4. Создаём векторное хранилище
        vector_store = create_vector_store(all_chunks)
        logger.info("Reindexing completed successfully")
        return vector_store
        
    except FileNotFoundError as e:
        logger.error(f"File not found: {e}")
        return None
    except Exception as e:
        logger.error(f"Error during reindexing: {e}", exc_info=True)
        return None
```

#### 5.5. Обновите импорты в `bot.py`

Измените импорт в `src/bot.py`:

```python
# Было:
from indexer import reindex_all

# Стало:
from indexer_with_json import reindex_all
```

#### 5.6. Протестируйте работу

1. **Перезапустите бота:**
   ```bash
   make run
   ```

2. **Проверьте логи** — должно быть что-то вроде:
   ```
   INFO - Loaded 168 Q&A pairs from JSON
   INFO - Total chunks to index: 545 (PDF: 377, JSON: 168)
   ```

3. **Задайте вопросы про карты:**
   - "Как заказать карту?"
   - "Какие документы нужны для получения карты?"
   - "Сколько делают карту?"
   - "Как активировать карту?"

4. **Сделайте скриншот** диалога с вопросами про карты для отчёта

**Чеклист результата:**
- ✅ Создан файл `src/indexer_with_json.py`
- ✅ JSON документы загружаются и индексируются
- ✅ Бот отвечает на вопросы про карты на основе JSON
- ✅ Сделан скриншот работы

---

### Шаг 6Б: Качество эмбеддингов решает многое

**Что нужно сделать:**

Если вы заметили, что бот перестал находить ответы на вопросы или качество ответов сильно снизилось, возможно проблема в качестве эмбеддингов. 

#### 6.1. Почему это важно

**Эмбеддинги** — это численные представления текста в векторном пространстве. От качества эмбеддингов напрямую зависит:
- Насколько точно будут найдены релевантные чанки
- Понимание семантической близости вопроса и документов
- Качество работы RAG-системы в целом

**Признаки проблем с эмбеддингами:**
- Бот не находит очевидные ответы из документов
- Возвращает нерелевантные чанки
- Плохо работает с русским языком

#### 6.2. Выбор качественной модели эмбеддингов

**Где искать лучшие модели:**

Воспользуйтесь **MTEB Leaderboard** — официальным бенчмарком для embedding моделей:
- 🔗 https://huggingface.co/spaces/mteb/leaderboard

На этом лидерборде модели ранжированы по качеству на различных задачах, включая retrieval (поиск).

**Рекомендуемые модели для русского языка:**

1. **OpenAI text-embedding-3-large** ✅
   - Отличное качество для русского языка
   - Доступна через OpenRouter
   - Проверенная и стабильная

2. **multilingual-e5-large** (для продакшена)
   - Хорошая бесплатная альтернатива
   - Требует локального развертывания
   pull ollama aroxima/multilingual-e5-large-instruct:latest

#### 6.3. Переключение на OpenAI embeddings через OpenRouter

**Обновите `.env` файл:**

```bash
# OpenRouter с качественными эмбеддингами
OPENAI_API_KEY=sk-or-v1-your-key-here
OPENAI_BASE_URL=https://openrouter.ai/api/v1
MODEL=openai/gpt-oss-120b:free
MODEL_QUERY_TRANSFORM=openai/gpt-oss-120b:free
EMBEDDING_MODEL=openai/text-embedding-3-large
```

#### 6.4. Альтернатива: Использование Ollama для эмбеддингов

Если вы хотите использовать локальные эмбеддинги через Ollama (бесплатно, без API ключей):

**Шаг 1: Скачайте модель эмбеддингов:**

```bash
ollama pull aroxima/multilingual-e5-large-instruct:latest
```

**Шаг 2: Установите пакет для Ollama:**

```bash
uv pip install langchain-ollama
```

**Шаг 3: Обновите функцию создания векторного хранилища:**

Замените импорт эмбеддингов:

```python
# Было:
from langchain_openai import OpenAIEmbeddings

# Стало:
from langchain_ollama import OllamaEmbeddings
```

Обновите функцию создания векторного хранилища:

```python
def create_vector_store(chunks: list):
    """Создание векторного хранилища"""
    embeddings = OllamaEmbeddings(
        model=config.EMBEDDING_MODEL
    )
    vector_store = InMemoryVectorStore.from_documents(
        documents=chunks,
        embedding=embeddings
    )
    logger.info(f"Created vector store with {len(chunks)} chunks")
    return vector_store
```

**Шаг 4: Обновите `.env`:**

```bash
EMBEDDING_MODEL=aroxima/multilingual-e5-large-instruct:latest
```

**Преимущества Ollama embeddings:**
- ✅ Бесплатно, работает локально
- ✅ Не нужны API ключи
- ✅ Хорошее качество для русского языка
- ❌ Требует установки Ollama и модели (~500MB-1GB)

#### 6.5. Протестируйте разницу

1. **Сохраните текущую конфигурацию** (с Fireworks эмбеддингами)

2. **Переключитесь на OpenRouter** с OpenAI эмбеддингами ИЛИ **на Ollama** с локальными эмбеддингами

3. **Перезапустите бота** (произойдет переиндексация):
   ```bash
   make run
   ```

4. **Задайте те же вопросы** и сравните:
   - Находятся ли релевантные документы?
   - Стали ли ответы более точными?
   - Улучшилась ли работа с уточняющими вопросами?

5. **Запишите наблюдения:**
   - Какая модель эмбеддингов дала лучший результат?
   - Изменилось ли качество ответов?
   - Какую модель вы рекомендуете для продакшена?

**Чеклист результата:**
- ✅ Изучен MTEB Leaderboard
- ✅ Протестированы разные модели эмбеддингов (минимум 2: Fireworks и OpenRouter/Ollama)
- ✅ Записаны наблюдения для отчёта

---

### Шаг 7Б: Подготовка отчета

Переходите к [Финальному шагу: Подготовка отчета](#финальный-шаг-подготовка-отчета) ниже.

---

## Вариант РАСШИРЕННЫЙ: Разработка с нуля

### Шаг 2Р: AI-driven разработка RAG-ассистента

**Что нужно сделать:**

1. **Создайте проект с нуля**, пройдя весь путь AI-driven разработки, используя подход из модуля 3.

**Два подхода на выбор:**

   **а) С чистого листа:**
   - Создайте новую папку `05-rag-langchain`
   - Следуйте методологии из ДЗ-3 (idea.md, vision.md, tasklist.md)
   - Используйте промпт ниже для формирования идеи

   **б) Модернизация проекта из модуля 3:**
   - Скопируйте ваш проект из `03-aidd/` в `05-rag-langchain/`
   - Обновите документацию (idea.md, vision.md)
   - Создайте новую итерацию в tasklist.md для добавления RAG
   - Проведите модернизацию через AIDD

2. **Используйте референсную тетрадку** как основу для реализации RAG:

**🤖 Промпт для AI-ассистента:**

```
Развиваем проект.
Я делаю RAG-ассистента на базе LangChain, который работает через Telegram бота, ведет диалог с пользователем, отвечая на его вопросы используя RAG.

Функциональные требования:

Индексация данных
Команда /index инициирует процесс полной переиндексации данных (очистка и загрузка с нуля)
При старте бота также запускается переиндексация
Данные лежат в директории @data в файлах PDF
Из файлов формируются документы и помещаются в InMemory векторную базу
Команда /index_status проверяет статус индексации (количество чанков, статус)

Ведение диалога
На каждое задаваемое пользователем сообщение проходим этап трансформации запроса на основе истории сообщений
Извлекаем топ-K чанков из ретривера (K задается в конфиге через RETRIEVER_K)
Передаем контекст в языковую модель для формирования ответа (динамически добавляем как контекст на последний вопрос)
Языковая модель дает ответ на основе истории переписки и предоставленного контекста
История диалога хранится сразу в формате LangChain messages (HumanMessage, AIMessage, SystemMessage), а не в словарях dict

Ретривер
Извлечение чанков через retriever векторной БД, заполненной на этапе индексации
Форматирование чанков с метаданными (источник, номер страницы) для лучшей прозрачности

## Важные указания по реализации:
- В качестве референса исходного раг пайплайна используй ноутбук @naive-rag.ipynb
векторная бд InMemoryVectorStore
финальная цепочка rag_query_transform_chain 
- Название моделей задаются в .env
- Поддерживай модульную организацию кода
- Обязательные технологии: Python, uv для управления зависимостями и вирт окружением, make для сборки проекта, для интеграции с Telegram Bot API используй async aiogram и метод polling

## Твоя задача:
Необходимо обновить idea.md, vision.md с учетом новых функциональных требований.
Необходимо спланировать итерации разработки и обновить tasklist.md следующим образом:
- существующие итераици сгруппировать в завершенный спринт 1
- создать новый спринт и включить в него итерации по реализации RAG-ассистента

ВАЖНО:  KISS, YAGNI, простота, понятность, чистота кода без оверинжиниринга
```

3. **Реализуйте базовый RAG** с помощью AI-ассистента следуя документации и tasklist.md

---

### Шаг 3Р: Эксперименты и доработка

**Что нужно сделать:**

1. **Выполните эксперименты с индексацией** (аналогично [Шагу 4Б](#шаг-4б-эксперименты-с-индексацией-pdf))
   - Попробуйте разные размеры чанков
   - Поэкспериментируйте с сепараторами
   - Запишите наблюдения

2. **Добавьте поддержку JSON датасета** (аналогично [Шагу 5Б](#шаг-5б-добавление-json-датасета-с-вопросами-ответами))
   - Используйте JSONLoader для загрузки пар вопрос-ответ
   - Объедините с индексацией PDF
   - Протестируйте на вопросах про карты
   - Сделайте скриншот

3. **Протестируйте качество эмбеддингов** (аналогично [Шагу 6Б](#шаг-6б-качество-эмбеддингов-решает-многое))
   - Сравните разные модели эмбеддингов
   - Оцените влияние на качество ответов
   - Запишите наблюдения в отчёт

---

### Шаг 4Р: Подготовка отчета

Переходите к [Финальному шагу: Подготовка отчета](#финальный-шаг-подготовка-отчета) ниже.

---

## Финальный шаг: Подготовка отчета

*Для обеих версий задания*

**Что нужно сделать:**

1. **Подготовьте скриншот:**
   - Работа бота с вопросами по JSON датасету (карты)

2. **Создайте папку для скриншотов:**
   ```bash
   mkdir screenshots
   ```
   Сохраните все скриншоты в эту папку.

3. **Используйте AI для создания отчета:**

**🤖 Промпт для создания отчета:**

```
@README.md @vision.md

Создай файл report.md в корне проекта с отчётом о выполнении задания.

Включи в отчёт:
- Название проекта и краткое описание
- Вариант задания (базовый/расширенный)
- Реализованные возможности (список с чекбоксами)
- Технологический стек 
- Используемые модели 

ВАЖНО — Эксперименты с индексацией:
- Описание экспериментов с разными размерами чанков
- Какие параметры пробовали 
- Наблюдения и выводы: какая стратегия оказалась лучше для банковских документов

ВАЖНО — Работа с JSON датасетом:
- Как реализовали загрузку JSON (JSONLoader)
- Скриншот работы с вопросами про карты

ВАЖНО — Сравнение моделей эмбеддингов:
- Какие модели эмбеддингов тестировали (Fireworks vs OpenRouter/OpenAI)
- Таблица сравнения качества ответов
- Выводы: какая модель эмбеддингов лучше для русского языка

Скриншоты прикрепи в виде markdown-ссылок на screenshots/.
```

**Чеклист результата:**
- ✅ Создан файл `report.md`
- ✅ Скриншоты сохранены и подключены
- ✅ Описаны эксперименты с индексацией с наблюдениями
- ✅ Добавлен скриншот работы с JSON датасетом
- ✅ Добавлено сравнение моделей эмбеддингов с выводами
- ✅ Описаны все реализованные возможности

---

## Шаг 10: Финальная фиксация и отправка

**Что нужно сделать:**

1. **Закомитьте все изменения в Git:**
   ```bash
   cd /путь/к/вашему/sber-agents
   git add 05-rag-langchain/
   git commit -m "Выполнено ДЗ-5: RAG с LangChain"
   git push
   ```

2. **Обновите прогресс на холсте:**
   - Перейдите на холст: https://app.holst.so/share/b/622fb72a-9b03-4f71-aeb8-013eb3afd151?objectId=a8b545aa-f2ed-489b-a164-c371e3e85e63
   - Отметьте выполнение ДЗ-05: поставьте ✅ в соответствующей колонке
   - **Загрузите скриншот работающего бота** в ячейку вашего задания на холсте
   - Обновите общий прогресс: 5/12

3. **Пройдите опрос обратной связи по Модулю 5:**
   - Ссылка на опрос: (будет предоставлена в чате курса)

4. **Отпишитесь о выполнении в Telegram:**
   - Перейдите в топик **"ДЗ-5 | RAG с LangChain"**
   - Используйте шаблон ниже

---

## 📝 Шаблон сообщения для Telegram

```
[СКРИНШОТ РАБОТЫ БОТА]

✅ Выполнил задание №5

📦 Репозиторий: https://github.com/ваш-username/sber-agents/tree/main/05-rag-langchain/

📋 Вариант: [Базовый / Расширенный]

🔬 Эксперименты с индексацией: [краткий вывод - что сработало лучше]
✅ JSON датасет (вопросы про карты): [реализовано / не реализовано]
📊 Сравнение эмбеддингов: [какие модели тестировали, результат]

API:
- Провайдер: [Fireworks.ai / OpenRouter]
- Модель: [gpt-oss-120b]
- Embeddings: [qwen3-embedding-8b / text-embedding-3-large]

🛠 AI-coding: [IDE] + [LLM модель]

💡 Главный инсайт: [1-2 предложения про RAG, эксперименты с чанкингом, эмбеддингами или JSON датасетом]

🔗 (Опционально) Ссылка на бота: @your_bot_name
```

---

**Успехов в освоении RAG и LangChain! 🚀**

*По вопросам обращайтесь в чат курса или к преподавателю.*

