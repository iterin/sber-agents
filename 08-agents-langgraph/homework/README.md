# Домашнее задание №8: ReAct Агенты с LangChain

## 🎯 Описание задания

В этом задании вы создадите ReAct агента (Reason + Act), который самостоятельно принимает решения о вызове инструментов. Агент будет работать с RAG-поиском по документам и дополнительными инструментами по вашему выбору.

**Цели:**
- Понять разницу между простыми цепочками и агентами
- Научиться создавать агентов с помощью LangChain
- Добавлять пользовательские инструменты
- Анализировать работу агента через трейсинг

**Важно:**
> **Версии задания:** Вы можете выбрать одну из двух версий:
> - **Базовая версия** — работа с готовым проектом из папки `live/`
> - **Расширенная версия** — AI-driven добавление агента в проект из ДЗ-7

---

## Шаг 0: Изучение материалов

**Что нужно сделать:**

1. **Изучите практические тетрадки:**
   - `08-agents-langgraph/slides/notebooks/agentic-rag.ipynb` — агентный подход к RAG
   - `08-agents-langgraph/slides/notebooks/agent.ipynb` — создание ReAct агента
   - Обратите внимание на:
     - Использование `create_react_agent()` (LangChain 1.0 подход)
     - Структуру инструментов (tools)
     - MemorySaver для хранения истории диалогов

2. **Изучите live-проект:**
   - Посмотрите реализацию в `08-agents-langgraph/live/`
   - Обратите внимание на:
     - `src/agent.py` — создание агента через `create_react_agent()`
     - `src/tools.py` — определение инструмента `rag_search`
     - `prompts/agent_system.txt` — системный промпт агента

3. **Ключевые концепции:**

**ReAct паттерн:**
- **Reason (Think)** — агент анализирует вопрос
- **Act** — вызывает нужный инструмент
- **Observe** — получает результат инструмента
- **Respond** — формирует итоговый ответ

**Отличие от цепочки:**
- Цепочка всегда вызывает RAG для каждого вопроса
- Агент сам решает когда нужен поиск, а когда можно ответить напрямую

---

## Вариант БАЗОВЫЙ: Работа с готовым проектом

### Шаг 1Б: Подготовка рабочего пространства

**Что нужно сделать:**

1. Перейдите в ваш репозиторий `sber-agents`:
   ```bash
   cd путь/к/вашему/sber-agents
   ```

2. Создайте папку `08-agents-langgraph`:
   ```bash
   mkdir -p 08-agents-langgraph
   cd 08-agents-langgraph
   ```

---

### Шаг 2Б: Копирование проекта

**Что нужно сделать:**

1. Скопируйте файлы проекта из директории `live`:
   ```bash
   # Находясь в папке 08-agents-langgraph
   cp -r ../08-agents-langgraph/live/* .
   ```

2. Откройте эту папку как проект в вашей IDE

---

### Шаг 3Б: Настройка и запуск

**Что нужно сделать:**

1. **Создайте `.env` файл:**
   ```bash
   cp env.example .env
   ```

2. **Настройте переменные окружения**

3. **Установите зависимости:**
   ```bash
   make install
   ```

4. **Запустите бота:**
   ```bash
   make run
   ```

**Чеклист результата:**
- ✅ Бот запущен и работает
- ✅ Документы проиндексированы
- ✅ В логах видно успешную индексацию

---

### Шаг 4Б: Тестирование агента

**Что нужно сделать:**

1. **Задайте разные типы вопросов:**

   **Простые вопросы (без RAG):**
   ```
   Привет!
   Спасибо за помощь
   Как дела?
   ```
   
   **Вопросы требующие поиска:**
   ```
   Какие получить кредит?
   Какая открыть вклад?
   ```

2. **Проанализируйте работу агента:**
   - Откройте LangSmith → Projects → ваш проект
   - Перейдите в раздел **Threads**
   - Найдите Thread с вашим `chat_id` из Telegram
   - Изучите трейсы:
     - Когда агент вызывал инструмент `rag_search`?
     - Когда отвечал напрямую без инструментов?

3. **Посмотрите логи:**
   - Должны быть записи о вызовах инструментов

4. **(Опционально) Запустите evaluation:**
   ```
   /evaluate_dataset
   ```
   - Проанализируйте результаты в LangSmith
   - Заметили изменения от прошлых запусков (ДЗ-7)?

**Чеклист результата:**
- ✅ Протестированы разные типы вопросов
- ✅ Изучены трейсы в LangSmith Threads
- ✅ Понятна логика работы агента

---

### Шаг 5Б: Добавление собственного инструмента

**Что нужно сделать:**

Реализуйте дополнительный инструмент и добавьте его агенту.

**Варианты инструментов:**

1. **Конвертация валют:**
   - Реальные данные с API (например, exchangerate-api.com)
   - Или заглушка с фиксированными курсами

2. **Свой вариант:**
   - Любой другой инструмент на ваш выбор

**Примерная структура инструмента:**

```python
@tool
def currency_converter(amount: float, from_currency: str, to_currency: str) -> str:
    """
    Конвертирует сумму из одной валюты в другую.
    
    Args:
        amount: Сумма для конвертации
        from_currency: Исходная валюта (USD, EUR, RUB)
        to_currency: Целевая валюта (USD, EUR, RUB)
    
    Returns:
        Строка с результатом конвертации
    """
    # Ваша реализация
    pass
```

**Как добавить:**
1. Откройте `src/tools.py`
2. Добавьте новый инструмент с декоратором `@tool`
3. Добавьте инструмент в список при создании агента в `src/agent.py`

**Чеклист результата:**
- ✅ Инструмент реализован
- ✅ Инструмент добавлен в агента
- ✅ Протестирован через бота
- ✅ Работа инструмента видна в LangSmith

---

### Шаг 6Б: Подготовка отчета

Переходите к [Финальному шагу: Подготовка отчета](#финальный-шаг-подготовка-отчета) ниже.

---

## Вариант РАСШИРЕННЫЙ: Доработка проекта из ДЗ-7

### Шаг 2Р: Доработка проекта

**Что нужно сделать:**

1. **Скопируйте ваш проект из ДЗ-7:**
   ```bash
   # Находясь в корне репозитория
   cp -r 07-advanced-rag 08-agents-langgraph
   cd 08-agents-langgraph
   ```

2. **Доработайте проект, добавив функционал ReAct агента**

Вы можете воспользоваться своей IDE с любым AI-кодовым агентом для создания новой версии проекта. Это реальная ситуация из практики разработки: мы экспериментируем с новыми техниками в Jupyter тетрадках, затем оборачиваем проверенные подходы в рабочий код для продакшена.

**Референс для реализации:**
- Используйте учебную тетрадку  `08-agents-langgraph/slides/notebooks/agent.ipynb` как основу для создания агента

**🤖 Промпт-подсказка для AI-ассистента:**

```
Развиваем проект до автономного ReAct агента банковсого обслуживания клиентов.
Агент должен обладать инструментом:
- rag_search - использует текущие варианты ритриверов (semantic/hybrid/hybrid_reranker), узел query_transformation не требуется, так как агент будет сам формировать поисковые фразы для вызова инструмента

Мы модернизируем наш проект:
- оформляем наш ретриевер в виде tool с описанием инструмента, аргументов и возвращаемых значений
- текущий Evaluation RAG должен продолжать функционировать для этого нужно сохранить в результатах перечень использованных для ответа документов
- точка входа теперь ии-агент с перечнем инструментов и системным промптом в котором есть четкая инструкция в каком случае вызывать данный инструмент, приведены few-shot примеры и даны подсказки по использованию инструмента

## Важные указания по реализации:
- Опирайся на референсную тетрадку docs/references/agent.ipynb , в части подхода к обертке инструмента tool, документированию описания и аргументов, а также создания ии-агента используя create_agent из LangChain 1.0 (раздел 10 тетрадки)
- Используй  @LANGCHAIN Python SDK  для референса кода по работе с API langchain, если не найдешь необходимых примеров в тетрадке
- Поддерживай модульную организацию кода
- Обязательные технологии: Python, uv для управления зависимостями и вирт окружением, make для сборки проекта, для интеграции с Telegram Bot API используй async aiogram и метод polling
- СОЗДАНИЕ АГЕНТА: Используй create_agent() из LangChain 1.0 с MemorySaver.
- ИНСТРУМЕНТ RAG_SEARCH: Возвращай {"sources": [...]}. Каждый source содержит: source (имя файла), page_content (полный текст - обязательно для RAGAS), page (номер страницы только для PDF). Используй ensure_ascii=False.
- ПОЛУЧЕНИЕ ОТВЕТА: В agent_answer используй bank_agent.stream() с stream_mode="values". Создай функцию для логирования каждого шага. Добавь warning при пустом AIMessage без tool_calls. Fallback для пустых ответов обязателен.
- ИЗВЛЕЧЕНИЕ ИСТОЧНИКОВ: Функция извлечения источников из текущего ответа должна собирать documents только после последнего HumanMessage (текущий запрос, не вся история). Собирай все ToolMessage с rag_search из текущего запроса.
- EVALUATION: Функция evaluate_dataset() полностью async. Внутри async def target(). Правильное использование aevaluate: сначала experiment_results = await client.aevaluate(...), потом async for result in experiment_results. Для RAGAS contexts извлекай page_content из documents. Уникальный chat_id присваивай каждому вызову, так как у нас история диалога храниится в MemorySaver.


## Твоя задача:
Необходимо обновить idea.md, vision.md с учетом новых функциональных требований.
Необходимо спланировать итерации разработки и обновить tasklist.md, создав новый спринт.

ВАЖНО:  KISS, YAGNI, простота, понятность, чистота кода без оверинжиниринга


---
```

---

### Шаг 3Р: Настройка, запуск и тестирование

**Что нужно сделать:**

Выполните задания из базового варианта, начиная с [Шага 3Б: Настройка и запуск](#шаг-3б-настройка-и-запуск).

---

## Финальный шаг: Подготовка отчета

*Для обеих версий задания*

**Что нужно сделать:**

1. **Сделайте скриншот:**
   - Из Telegram: переписка с ботом, где видно вызовы инструментов
   - ИЛИ из LangSmith: страница Thread с трейсами вызовов инструментов

2. **Создайте файл `report.md` с наблюдениями:**
   - Какой инструмент вы реализовали?
   - Как агент принимает решения о вызове инструментов?
   - Чем агент отличается от простой RAG-цепочки?
   - Какие преимущества и ограничения вы заметили?

**Чеклист результата:**
- ✅ Скриншот сохранен
- ✅ Файл `report.md` создан
- ✅ Наблюдения зафиксированы

---

## Шаг 10: Финальная фиксация и отправка

**Что нужно сделать:**

1. **Закомитьте все изменения в Git:**
   ```bash
   cd /путь/к/вашему/sber-agents
   git add 08-agents-langgraph/
   git commit -m "Выполнено ДЗ-8: ReAct агенты с LangChain"
   git push
   ```

2. **Обновите прогресс на холсте:**
   - Перейдите на холст: https://app.holst.so/share/b/622fb72a-9b03-4f71-aeb8-013eb3afd151?objectId=a8b545aa-f2ed-489b-a164-c371e3e85e63
   - Отметьте выполнение ДЗ-08: поставьте ✅
   - Загрузите скриншот работы агента
   - Обновите общий прогресс: 8/12

3. **Пройдите опрос обратной связи по Модулю 8:**
   - Ссылка на опрос: https://forms.gle/CLZtKYAjTr4EPy7R7

4. **Отпишитесь о выполнении в Telegram:**
   - Перейдите в топик **"ДЗ-8 | ReAct агент"**
   - Используйте шаблон ниже

---

## 📝 Шаблон сообщения для Telegram

```
[СКРИНШОТ РАБОТЫ АГЕНТА С ВЫЗОВАМИ ИНСТРУМЕНТОВ]

✅ Выполнил задание №8

📦 Репозиторий: https://github.com/ваш-username/sber-agents/tree/main/08-agents-langgraph/

📋 Вариант: [Базовый / Расширенный]

🔧 Реализованный инструмент:
- Название: [currency_converter / свой вариант]
- Назначение: [краткое описание]

🤖 Наблюдения:[Проблемы/сбои/качество работы]

🛠 AI-coding: [IDE] + [LLM модель]

📋 Регистрация на холсте: обновлена (8/12)

📝 Опрос пройден: ✅

💡 Главный инсайт: [1-2 предложения о работе агента]

🔗 (Опционально) Ссылка на бота: @your_bot_name
```

---

**Успехов в освоении ReAct агентов! 🚀**

*По вопросам обращайтесь в чат курса*

